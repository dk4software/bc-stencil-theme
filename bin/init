#!/bin/bash
docker-compose -f docker-compose.yml up -d --remove-orphans --build
docker-compose -f docker-compose.yml run stencil npm install
docker-compose -f docker-compose.yml run stencil stencil init
bin/restart